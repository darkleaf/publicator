;; 1. db.Dockerfile
;;    добавить 2 базы
;; 2. неймспейс для миграций, функция принимает data_source
;; 3. компонент для коннекта
;; 4. тесты не испльзуют component, неймспейс с коннектом
;; 5. для ci сделать таску через alias( lein run -m ... ) для запуска миграций
;;    запускать одной командой через lein do
;; 6. в user.clj добавить хелрер для миграции

;; нужно делать мемоизацияю пула
;; чтобы при релоаде он не пересоздавался
